// Package nav owns the nav-state grammar: the signed NavState codes, encode/decode, and the button helper. It imports no screen package, keeping the dependency graph acyclic (nav <- screens <- main).
package nav
